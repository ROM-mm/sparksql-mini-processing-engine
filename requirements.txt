# Spark e PySpark
# Versão 4.1.0.dev3 (development preview com suporte a SDP)
pyspark==4.1.0.dev3

# Alternativas (descomente se necessário):
# pyspark==4.0.0  # Versão estável (sem SDP)
# pyspark>=4.1.0a1  # Outra versão preview
# pip install --pre pyspark  # Última versão preview disponível

pyyaml>=6.0

# Airflow
# Versão fixa 2.10.5 para garantir compatibilidade e reprodutibilidade
apache-airflow==2.10.5
apache-airflow-providers-apache-spark>=4.0.0

# Utilitários
python-dateutil>=2.8.2
